# Generated by cloudcoil-model-codegen v0.7.1
# DO NOT EDIT

from typing import (
    Annotated,
    Any,
    Callable,
    ClassVar,
    Literal,
    overload,
)

from cloudcoil.apimachinery import ObjectMeta
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource
from pydantic import ConfigDict, Field


class BrokerSpecConfig(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerSpecConfig"]:
            return BrokerSpecConfig

        def build(self) -> "BrokerSpecConfig":
            return BrokerSpecConfig(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "API version of the referent."
            return self._set("api_version", value)

        def kind(self, value: str | None, /) -> Self:
            "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["BrokerSpecConfig.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerSpecConfig.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerSpecConfig."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BrokerSpecConfig", Builder]:
        return GenericListBuilder[BrokerSpecConfig, BrokerSpecConfig.Builder]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "API version of the referent."
    kind: str | None = None
    "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    name: str | None = None
    "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    namespace: str | None = None
    "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out."


class BrokerSpecDeliveryDeadLetterSinkRef(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerSpecDeliveryDeadLetterSinkRef"]:
            return BrokerSpecDeliveryDeadLetterSinkRef

        def build(self) -> "BrokerSpecDeliveryDeadLetterSinkRef":
            return BrokerSpecDeliveryDeadLetterSinkRef(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "API version of the referent."
            return self._set("api_version", value)

        def kind(self, value: str | None, /) -> Self:
            "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["BrokerSpecDeliveryDeadLetterSinkRef.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerSpecDeliveryDeadLetterSinkRef.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerSpecDeliveryDeadLetterSinkRef."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["BrokerSpecDeliveryDeadLetterSinkRef", Builder]:
        return GenericListBuilder[
            BrokerSpecDeliveryDeadLetterSinkRef,
            BrokerSpecDeliveryDeadLetterSinkRef.Builder,
        ]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "API version of the referent."
    kind: str | None = None
    "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    name: str | None = None
    "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    namespace: str | None = None
    "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out."


class BrokerStatusAddress(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerStatusAddress"]:
            return BrokerStatusAddress

        def build(self) -> "BrokerStatusAddress":
            return BrokerStatusAddress(**self._attrs)

        def name(self, value: str | None, /) -> Self:
            return self._set("name", value)

        def url(self, value: str | None, /) -> Self:
            return self._set("url", value)

        def ca_certs(self, value: str | None, /) -> Self:
            return self._set("ca_certs", value)

        def audience(self, value: str | None, /) -> Self:
            return self._set("audience", value)

    class BuilderContext(BuilderContextBase["BrokerStatusAddress.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerStatusAddress.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerStatusAddress."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BrokerStatusAddress", Builder]:
        return GenericListBuilder[BrokerStatusAddress, BrokerStatusAddress.Builder]()

    name: str | None = None
    url: str | None = None
    ca_certs: Annotated[str | None, Field(alias="CACerts")] = None
    audience: str | None = None


class BrokerStatusAddressesItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerStatusAddressesItem"]:
            return BrokerStatusAddressesItem

        def build(self) -> "BrokerStatusAddressesItem":
            return BrokerStatusAddressesItem(**self._attrs)

        def name(self, value: str | None, /) -> Self:
            return self._set("name", value)

        def url(self, value: str | None, /) -> Self:
            return self._set("url", value)

        def ca_certs(self, value: str | None, /) -> Self:
            return self._set("ca_certs", value)

        def audience(self, value: str | None, /) -> Self:
            return self._set("audience", value)

    class BuilderContext(BuilderContextBase["BrokerStatusAddressesItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerStatusAddressesItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerStatusAddressesItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BrokerStatusAddressesItem", Builder]:
        return GenericListBuilder[BrokerStatusAddressesItem, BrokerStatusAddressesItem.Builder]()

    name: str | None = None
    url: str | None = None
    ca_certs: Annotated[str | None, Field(alias="CACerts")] = None
    audience: str | None = None


class BrokerStatusPoliciesItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerStatusPoliciesItem"]:
            return BrokerStatusPoliciesItem

        def build(self) -> "BrokerStatusPoliciesItem":
            return BrokerStatusPoliciesItem(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "The API version of the applied EventPolicy. This indicates, which version of EventPolicy is supported by the resource."
            return self._set("api_version", value)

        def name(self, value: str | None, /) -> Self:
            "The name of the applied EventPolicy"
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["BrokerStatusPoliciesItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerStatusPoliciesItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerStatusPoliciesItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BrokerStatusPoliciesItem", Builder]:
        return GenericListBuilder[BrokerStatusPoliciesItem, BrokerStatusPoliciesItem.Builder]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "The API version of the applied EventPolicy. This indicates, which version of EventPolicy is supported by the resource."
    name: str | None = None
    "The name of the applied EventPolicy"


class BrokerStatusConditionsItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerStatusConditionsItem"]:
            return BrokerStatusConditionsItem

        def build(self) -> "BrokerStatusConditionsItem":
            return BrokerStatusConditionsItem(**self._attrs)

        def last_transition_time(self, value: str | None, /) -> Self:
            "LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant)."
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "A human readable message indicating details about the transition."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "The reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def severity(self, value: str | None, /) -> Self:
            "Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error."
            return self._set("severity", value)

        def status(self, value: str, /) -> Self:
            "Status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type of condition."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["BrokerStatusConditionsItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerStatusConditionsItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerStatusConditionsItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BrokerStatusConditionsItem", Builder]:
        return GenericListBuilder[BrokerStatusConditionsItem, BrokerStatusConditionsItem.Builder]()

    last_transition_time: Annotated[str | None, Field(alias="lastTransitionTime")] = None
    "LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant)."
    message: str | None = None
    "A human readable message indicating details about the transition."
    reason: str | None = None
    "The reason for the condition\u0027s last transition."
    severity: str | None = None
    "Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error."
    status: str
    "Status of the condition, one of True, False, Unknown."
    type: str
    "Type of condition."


class TriggerSpecBrokerRef(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerSpecBrokerRef"]:
            return TriggerSpecBrokerRef

        def build(self) -> "TriggerSpecBrokerRef":
            return TriggerSpecBrokerRef(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "API version of the referent."
            return self._set("api_version", value)

        def kind(self, value: str | None, /) -> Self:
            "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/"
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["TriggerSpecBrokerRef.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerSpecBrokerRef.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerSpecBrokerRef."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerSpecBrokerRef", Builder]:
        return GenericListBuilder[TriggerSpecBrokerRef, TriggerSpecBrokerRef.Builder]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "API version of the referent."
    kind: str | None = None
    "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    name: str | None = None
    "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    namespace: str | None = None
    "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/"


class TriggerSpecDeliveryDeadLetterSinkRef(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerSpecDeliveryDeadLetterSinkRef"]:
            return TriggerSpecDeliveryDeadLetterSinkRef

        def build(self) -> "TriggerSpecDeliveryDeadLetterSinkRef":
            return TriggerSpecDeliveryDeadLetterSinkRef(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "API version of the referent."
            return self._set("api_version", value)

        def kind(self, value: str | None, /) -> Self:
            "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["TriggerSpecDeliveryDeadLetterSinkRef.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerSpecDeliveryDeadLetterSinkRef.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerSpecDeliveryDeadLetterSinkRef."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["TriggerSpecDeliveryDeadLetterSinkRef", Builder]:
        return GenericListBuilder[
            TriggerSpecDeliveryDeadLetterSinkRef,
            TriggerSpecDeliveryDeadLetterSinkRef.Builder,
        ]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "API version of the referent."
    kind: str | None = None
    "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    name: str | None = None
    "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    namespace: str | None = None
    "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out."


class TriggerSpecFilter(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerSpecFilter"]:
            return TriggerSpecFilter

        def build(self) -> "TriggerSpecFilter":
            return TriggerSpecFilter(**self._attrs)

        def attributes(self, value: dict[str, Any] | None, /) -> Self:
            "Attributes filters events by exact match on event context attributes. Each key in the map is compared with the equivalent key in the event context. An event passes the filter if all values are equal to the specified values.  Nested context attributes are not supported as keys. Only string values are supported."
            return self._set("attributes", value)

    class BuilderContext(BuilderContextBase["TriggerSpecFilter.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerSpecFilter.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerSpecFilter."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerSpecFilter", Builder]:
        return GenericListBuilder[TriggerSpecFilter, TriggerSpecFilter.Builder]()

    attributes: dict[str, Any] | None = None
    "Attributes filters events by exact match on event context attributes. Each key in the map is compared with the equivalent key in the event context. An event passes the filter if all values are equal to the specified values.  Nested context attributes are not supported as keys. Only string values are supported."


class TriggerSpecFiltersItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerSpecFiltersItem"]:
            return TriggerSpecFiltersItem

        def build(self) -> "TriggerSpecFiltersItem":
            return TriggerSpecFiltersItem(**self._attrs)

        def all(self, value: list[dict[str, Any]] | None, /) -> Self:
            "All evaluates to true if all the nested expressions evaluate to true. It must contain at least one filter expression."
            return self._set("all", value)

        def any(self, value: list[dict[str, Any]] | None, /) -> Self:
            "Any evaluates to true if at least one of the nested expressions evaluates to true. It must contain at least one filter expression."
            return self._set("any", value)

        def cesql(self, value: str | None, /) -> Self:
            "CESQL is a CloudEvents SQL expression that will be evaluated to true or false against each CloudEvent."
            return self._set("cesql", value)

        def exact(self, value: dict[str, Any] | None, /) -> Self:
            "Exact evaluates to true if the values of the matching CloudEvents attributes all exactly match with the associated value String specified (case-sensitive). The keys are the names of the CloudEvents attributes to be matched, and their values are the String values to use in the comparison. The attribute name and value specified in the filter express must not be empty strings."
            return self._set("exact", value)

        def not_(self, value: dict[str, Any] | None, /) -> Self:
            "Not evaluates to true if the nested expression evaluates to false."
            return self._set("not_", value)

        def prefix(self, value: dict[str, Any] | None, /) -> Self:
            "Prefix evaluates to true if the values of the matching CloudEvents attributes all start with the associated value String specified (case sensitive). The keys are the names of the CloudEvents attributes to be matched, and their values are the String values to use in the comparison. The attribute name and value specified in the filter express must not be empty strings."
            return self._set("prefix", value)

        def suffix(self, value: dict[str, Any] | None, /) -> Self:
            "Suffix evaluates to true if the values of the matching CloudEvents attributes all end with the associated value String specified (case sensitive). The keys are the names of the CloudEvents attributes to be matched, and their values are the String values to use in the comparison. The attribute name and value specified in the filter express must not be empty strings."
            return self._set("suffix", value)

    class BuilderContext(BuilderContextBase["TriggerSpecFiltersItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerSpecFiltersItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerSpecFiltersItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerSpecFiltersItem", Builder]:
        return GenericListBuilder[TriggerSpecFiltersItem, TriggerSpecFiltersItem.Builder]()

    all: list[dict[str, Any]] | None = None
    "All evaluates to true if all the nested expressions evaluate to true. It must contain at least one filter expression."
    any: list[dict[str, Any]] | None = None
    "Any evaluates to true if at least one of the nested expressions evaluates to true. It must contain at least one filter expression."
    cesql: str | None = None
    "CESQL is a CloudEvents SQL expression that will be evaluated to true or false against each CloudEvent."
    exact: dict[str, Any] | None = None
    "Exact evaluates to true if the values of the matching CloudEvents attributes all exactly match with the associated value String specified (case-sensitive). The keys are the names of the CloudEvents attributes to be matched, and their values are the String values to use in the comparison. The attribute name and value specified in the filter express must not be empty strings."
    not_: Annotated[dict[str, Any] | None, Field(alias="not")] = None
    "Not evaluates to true if the nested expression evaluates to false."
    prefix: dict[str, Any] | None = None
    "Prefix evaluates to true if the values of the matching CloudEvents attributes all start with the associated value String specified (case sensitive). The keys are the names of the CloudEvents attributes to be matched, and their values are the String values to use in the comparison. The attribute name and value specified in the filter express must not be empty strings."
    suffix: dict[str, Any] | None = None
    "Suffix evaluates to true if the values of the matching CloudEvents attributes all end with the associated value String specified (case sensitive). The keys are the names of the CloudEvents attributes to be matched, and their values are the String values to use in the comparison. The attribute name and value specified in the filter express must not be empty strings."


class TriggerSpecSubscriberRef(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerSpecSubscriberRef"]:
            return TriggerSpecSubscriberRef

        def build(self) -> "TriggerSpecSubscriberRef":
            return TriggerSpecSubscriberRef(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "API version of the referent."
            return self._set("api_version", value)

        def kind(self, value: str | None, /) -> Self:
            "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["TriggerSpecSubscriberRef.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerSpecSubscriberRef.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerSpecSubscriberRef."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerSpecSubscriberRef", Builder]:
        return GenericListBuilder[TriggerSpecSubscriberRef, TriggerSpecSubscriberRef.Builder]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "API version of the referent."
    kind: str | None = None
    "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    name: str | None = None
    "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    namespace: str | None = None
    "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out."


class TriggerStatusAuth(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerStatusAuth"]:
            return TriggerStatusAuth

        def build(self) -> "TriggerStatusAuth":
            return TriggerStatusAuth(**self._attrs)

        def service_account_name(self, value: str | None, /) -> Self:
            "ServiceAccountName is the name of the generated service account used for this components OIDC authentication."
            return self._set("service_account_name", value)

        def service_account_names(self, value: list[str] | None, /) -> Self:
            "ServiceAccountNames is the list of names of the generated service accounts used for this components OIDC authentication."
            return self._set("service_account_names", value)

    class BuilderContext(BuilderContextBase["TriggerStatusAuth.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerStatusAuth.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerStatusAuth."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerStatusAuth", Builder]:
        return GenericListBuilder[TriggerStatusAuth, TriggerStatusAuth.Builder]()

    service_account_name: Annotated[str | None, Field(alias="serviceAccountName")] = None
    "ServiceAccountName is the name of the generated service account used for this components OIDC authentication."
    service_account_names: Annotated[list[str] | None, Field(alias="serviceAccountNames")] = None
    "ServiceAccountNames is the list of names of the generated service accounts used for this components OIDC authentication."


class TriggerStatusConditionsItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerStatusConditionsItem"]:
            return TriggerStatusConditionsItem

        def build(self) -> "TriggerStatusConditionsItem":
            return TriggerStatusConditionsItem(**self._attrs)

        def last_transition_time(self, value: str | None, /) -> Self:
            "LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant)."
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "A human readable message indicating details about the transition."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "The reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def severity(self, value: str | None, /) -> Self:
            "Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error."
            return self._set("severity", value)

        def status(self, value: str, /) -> Self:
            "Status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type of condition."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["TriggerStatusConditionsItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerStatusConditionsItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerStatusConditionsItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerStatusConditionsItem", Builder]:
        return GenericListBuilder[
            TriggerStatusConditionsItem, TriggerStatusConditionsItem.Builder
        ]()

    last_transition_time: Annotated[str | None, Field(alias="lastTransitionTime")] = None
    "LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant)."
    message: str | None = None
    "A human readable message indicating details about the transition."
    reason: str | None = None
    "The reason for the condition\u0027s last transition."
    severity: str | None = None
    "Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error."
    status: str
    "Status of the condition, one of True, False, Unknown."
    type: str
    "Type of condition."


class BrokerStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerStatus"]:
            return BrokerStatus

        def build(self) -> "BrokerStatus":
            return BrokerStatus(**self._attrs)

        @overload
        def address(
            self, value_or_callback: BrokerStatusAddress | None, /
        ) -> "BrokerStatus.Builder": ...

        @overload
        def address(
            self,
            value_or_callback: Callable[
                [BrokerStatusAddress.Builder],
                BrokerStatusAddress.Builder | BrokerStatusAddress,
            ],
            /,
        ) -> "BrokerStatus.Builder": ...

        @overload
        def address(
            self, value_or_callback: Never = ...
        ) -> "BrokerStatusAddress.BuilderContext": ...

        def address(self, value_or_callback=UNSET, /):
            "Broker is Addressable. It exposes the endpoint as an URI to get events delivered into the Broker mesh."
            if self._in_context and value_or_callback is UNSET:
                context = BrokerStatusAddress.BuilderContext()
                context._parent_builder = self
                context._field_name = "address"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerStatusAddress.builder())
                if isinstance(output, BrokerStatusAddress.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("address", value)

        @overload
        def addresses(
            self, value_or_callback: list[BrokerStatusAddressesItem] | None, /
        ) -> "BrokerStatus.Builder": ...

        @overload
        def addresses(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[BrokerStatusAddressesItem, BrokerStatusAddressesItem.Builder]],
                GenericListBuilder[BrokerStatusAddressesItem, BrokerStatusAddressesItem.Builder]
                | list[BrokerStatusAddressesItem],
            ],
            /,
        ) -> "BrokerStatus.Builder": ...

        @overload
        def addresses(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[BrokerStatusAddressesItem.Builder]: ...

        def addresses(self, value_or_callback=UNSET, /):
            "Broker is Addressable. It exposes the endpoints as URIs to get events delivered into the Broker mesh."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[BrokerStatusAddressesItem.Builder]()
                context._parent_builder = self
                context._field_name = "addresses"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerStatusAddressesItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("addresses", value)

        def annotations(self, value: dict[str, Any] | None, /) -> Self:
            "Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards."
            return self._set("annotations", value)

        @overload
        def policies(
            self, value_or_callback: list[BrokerStatusPoliciesItem] | None, /
        ) -> "BrokerStatus.Builder": ...

        @overload
        def policies(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[BrokerStatusPoliciesItem, BrokerStatusPoliciesItem.Builder]],
                GenericListBuilder[BrokerStatusPoliciesItem, BrokerStatusPoliciesItem.Builder]
                | list[BrokerStatusPoliciesItem],
            ],
            /,
        ) -> "BrokerStatus.Builder": ...

        @overload
        def policies(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[BrokerStatusPoliciesItem.Builder]: ...

        def policies(self, value_or_callback=UNSET, /):
            "List of applied EventPolicies"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[BrokerStatusPoliciesItem.Builder]()
                context._parent_builder = self
                context._field_name = "policies"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerStatusPoliciesItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("policies", value)

        @overload
        def conditions(
            self, value_or_callback: list[BrokerStatusConditionsItem] | None, /
        ) -> "BrokerStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        BrokerStatusConditionsItem, BrokerStatusConditionsItem.Builder
                    ]
                ],
                GenericListBuilder[BrokerStatusConditionsItem, BrokerStatusConditionsItem.Builder]
                | list[BrokerStatusConditionsItem],
            ],
            /,
        ) -> "BrokerStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[BrokerStatusConditionsItem.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Conditions the latest available observations of a resource\u0027s current state."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[BrokerStatusConditionsItem.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerStatusConditionsItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def dead_letter_sink_uri(self, value: str | None, /) -> Self:
            "DeadLetterSinkURI is the resolved URI of the dead letter sink that will be used as a fallback when not specified by Triggers."
            return self._set("dead_letter_sink_uri", value)

        def dead_letter_sink_ca_certs(self, value: str | None, /) -> Self:
            "Certification Authority (CA) certificates in PEM format according to https://www.rfc-editor.org/rfc/rfc7468."
            return self._set("dead_letter_sink_ca_certs", value)

        def dead_letter_sink_audience(self, value: str | None, /) -> Self:
            "OIDC audience of the dead letter sink."
            return self._set("dead_letter_sink_audience", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "ObservedGeneration is the \u0027Generation\u0027 of the Service that was last processed by the controller."
            return self._set("observed_generation", value)

    class BuilderContext(BuilderContextBase["BrokerStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BrokerStatus", Builder]:
        return GenericListBuilder[BrokerStatus, BrokerStatus.Builder]()

    address: BrokerStatusAddress | None = None
    "Broker is Addressable. It exposes the endpoint as an URI to get events delivered into the Broker mesh."
    addresses: list[BrokerStatusAddressesItem] | None = None
    "Broker is Addressable. It exposes the endpoints as URIs to get events delivered into the Broker mesh."
    annotations: dict[str, Any] | None = None
    "Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards."
    policies: list[BrokerStatusPoliciesItem] | None = None
    "List of applied EventPolicies"
    conditions: list[BrokerStatusConditionsItem] | None = None
    "Conditions the latest available observations of a resource\u0027s current state."
    dead_letter_sink_uri: Annotated[str | None, Field(alias="deadLetterSinkUri")] = None
    "DeadLetterSinkURI is the resolved URI of the dead letter sink that will be used as a fallback when not specified by Triggers."
    dead_letter_sink_ca_certs: Annotated[str | None, Field(alias="deadLetterSinkCACerts")] = None
    "Certification Authority (CA) certificates in PEM format according to https://www.rfc-editor.org/rfc/rfc7468."
    dead_letter_sink_audience: Annotated[str | None, Field(alias="deadLetterSinkAudience")] = None
    "OIDC audience of the dead letter sink."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "ObservedGeneration is the \u0027Generation\u0027 of the Service that was last processed by the controller."


class TriggerStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerStatus"]:
            return TriggerStatus

        def build(self) -> "TriggerStatus":
            return TriggerStatus(**self._attrs)

        def annotations(self, value: dict[str, Any] | None, /) -> Self:
            "Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards."
            return self._set("annotations", value)

        @overload
        def auth(
            self, value_or_callback: TriggerStatusAuth | None, /
        ) -> "TriggerStatus.Builder": ...

        @overload
        def auth(
            self,
            value_or_callback: Callable[
                [TriggerStatusAuth.Builder],
                TriggerStatusAuth.Builder | TriggerStatusAuth,
            ],
            /,
        ) -> "TriggerStatus.Builder": ...

        @overload
        def auth(self, value_or_callback: Never = ...) -> "TriggerStatusAuth.BuilderContext": ...

        def auth(self, value_or_callback=UNSET, /):
            "Auth provides the relevant information for OIDC authentication."
            if self._in_context and value_or_callback is UNSET:
                context = TriggerStatusAuth.BuilderContext()
                context._parent_builder = self
                context._field_name = "auth"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerStatusAuth.builder())
                if isinstance(output, TriggerStatusAuth.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("auth", value)

        @overload
        def conditions(
            self, value_or_callback: list[TriggerStatusConditionsItem] | None, /
        ) -> "TriggerStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        TriggerStatusConditionsItem, TriggerStatusConditionsItem.Builder
                    ]
                ],
                GenericListBuilder[TriggerStatusConditionsItem, TriggerStatusConditionsItem.Builder]
                | list[TriggerStatusConditionsItem],
            ],
            /,
        ) -> "TriggerStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[TriggerStatusConditionsItem.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Conditions the latest available observations of a resource\u0027s current state."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[TriggerStatusConditionsItem.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerStatusConditionsItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def dead_letter_sink_uri(self, value: str | None, /) -> Self:
            "DeadLetterSinkURI is the resolved URI of the dead letter sink for this Trigger, in case there is none this will fallback to it\u0027s Broker status DeadLetterSinkURI."
            return self._set("dead_letter_sink_uri", value)

        def dead_letter_sink_ca_certs(self, value: str | None, /) -> Self:
            "Certification Authority (CA) certificates in PEM format according to https://www.rfc-editor.org/rfc/rfc7468."
            return self._set("dead_letter_sink_ca_certs", value)

        def dead_letter_sink_audience(self, value: str | None, /) -> Self:
            "OIDC audience of the dead letter sink."
            return self._set("dead_letter_sink_audience", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "ObservedGeneration is the \u0027Generation\u0027 of the Service that was last processed by the controller."
            return self._set("observed_generation", value)

        def subscriber_uri(self, value: str | None, /) -> Self:
            "SubscriberURI is the resolved URI of the receiver for this Trigger."
            return self._set("subscriber_uri", value)

        def subscriber_ca_certs(self, value: str | None, /) -> Self:
            "Certification Authority (CA) certificates in PEM format according to https://www.rfc-editor.org/rfc/rfc7468."
            return self._set("subscriber_ca_certs", value)

        def subscriber_audience(self, value: str | None, /) -> Self:
            "OIDC audience of the subscriber."
            return self._set("subscriber_audience", value)

    class BuilderContext(BuilderContextBase["TriggerStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerStatus", Builder]:
        return GenericListBuilder[TriggerStatus, TriggerStatus.Builder]()

    annotations: dict[str, Any] | None = None
    "Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards."
    auth: TriggerStatusAuth | None = None
    "Auth provides the relevant information for OIDC authentication."
    conditions: list[TriggerStatusConditionsItem] | None = None
    "Conditions the latest available observations of a resource\u0027s current state."
    dead_letter_sink_uri: Annotated[str | None, Field(alias="deadLetterSinkUri")] = None
    "DeadLetterSinkURI is the resolved URI of the dead letter sink for this Trigger, in case there is none this will fallback to it\u0027s Broker status DeadLetterSinkURI."
    dead_letter_sink_ca_certs: Annotated[str | None, Field(alias="deadLetterSinkCACerts")] = None
    "Certification Authority (CA) certificates in PEM format according to https://www.rfc-editor.org/rfc/rfc7468."
    dead_letter_sink_audience: Annotated[str | None, Field(alias="deadLetterSinkAudience")] = None
    "OIDC audience of the dead letter sink."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "ObservedGeneration is the \u0027Generation\u0027 of the Service that was last processed by the controller."
    subscriber_uri: Annotated[str | None, Field(alias="subscriberUri")] = None
    "SubscriberURI is the resolved URI of the receiver for this Trigger."
    subscriber_ca_certs: Annotated[str | None, Field(alias="subscriberCACerts")] = None
    "Certification Authority (CA) certificates in PEM format according to https://www.rfc-editor.org/rfc/rfc7468."
    subscriber_audience: Annotated[str | None, Field(alias="subscriberAudience")] = None
    "OIDC audience of the subscriber."


class BrokerSpecDeliveryDeadLetterSink(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerSpecDeliveryDeadLetterSink"]:
            return BrokerSpecDeliveryDeadLetterSink

        def build(self) -> "BrokerSpecDeliveryDeadLetterSink":
            return BrokerSpecDeliveryDeadLetterSink(**self._attrs)

        @overload
        def ref(
            self, value_or_callback: BrokerSpecDeliveryDeadLetterSinkRef | None, /
        ) -> "BrokerSpecDeliveryDeadLetterSink.Builder": ...

        @overload
        def ref(
            self,
            value_or_callback: Callable[
                [BrokerSpecDeliveryDeadLetterSinkRef.Builder],
                BrokerSpecDeliveryDeadLetterSinkRef.Builder | BrokerSpecDeliveryDeadLetterSinkRef,
            ],
            /,
        ) -> "BrokerSpecDeliveryDeadLetterSink.Builder": ...

        @overload
        def ref(
            self, value_or_callback: Never = ...
        ) -> "BrokerSpecDeliveryDeadLetterSinkRef.BuilderContext": ...

        def ref(self, value_or_callback=UNSET, /):
            "Ref points to an Addressable."
            if self._in_context and value_or_callback is UNSET:
                context = BrokerSpecDeliveryDeadLetterSinkRef.BuilderContext()
                context._parent_builder = self
                context._field_name = "ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerSpecDeliveryDeadLetterSinkRef.builder())
                if isinstance(output, BrokerSpecDeliveryDeadLetterSinkRef.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("ref", value)

        def uri(self, value: str | None, /) -> Self:
            "URI can be an absolute URL(non-empty scheme and non-empty host) pointing to the target or a relative URI. Relative URIs will be resolved using the base URI retrieved from Ref."
            return self._set("uri", value)

        def ca_certs(self, value: str | None, /) -> Self:
            "Certification Authority (CA) certificates in PEM format that the source trusts when sending events to the sink."
            return self._set("ca_certs", value)

        def audience(self, value: str | None, /) -> Self:
            "Audience is the OIDC audience. This only needs to be set if the target is not an Addressable and thus the Audience can\u0027t be received from the target itself. If specified, it takes precedence over the target\u0027s Audience."
            return self._set("audience", value)

    class BuilderContext(BuilderContextBase["BrokerSpecDeliveryDeadLetterSink.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerSpecDeliveryDeadLetterSink.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerSpecDeliveryDeadLetterSink."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["BrokerSpecDeliveryDeadLetterSink", Builder]:
        return GenericListBuilder[
            BrokerSpecDeliveryDeadLetterSink, BrokerSpecDeliveryDeadLetterSink.Builder
        ]()

    ref: BrokerSpecDeliveryDeadLetterSinkRef | None = None
    "Ref points to an Addressable."
    uri: str | None = None
    "URI can be an absolute URL(non-empty scheme and non-empty host) pointing to the target or a relative URI. Relative URIs will be resolved using the base URI retrieved from Ref."
    ca_certs: Annotated[str | None, Field(alias="CACerts")] = None
    "Certification Authority (CA) certificates in PEM format that the source trusts when sending events to the sink."
    audience: str | None = None
    "Audience is the OIDC audience. This only needs to be set if the target is not an Addressable and thus the Audience can\u0027t be received from the target itself. If specified, it takes precedence over the target\u0027s Audience."


class TriggerSpecDeliveryDeadLetterSink(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerSpecDeliveryDeadLetterSink"]:
            return TriggerSpecDeliveryDeadLetterSink

        def build(self) -> "TriggerSpecDeliveryDeadLetterSink":
            return TriggerSpecDeliveryDeadLetterSink(**self._attrs)

        @overload
        def ref(
            self, value_or_callback: TriggerSpecDeliveryDeadLetterSinkRef | None, /
        ) -> "TriggerSpecDeliveryDeadLetterSink.Builder": ...

        @overload
        def ref(
            self,
            value_or_callback: Callable[
                [TriggerSpecDeliveryDeadLetterSinkRef.Builder],
                TriggerSpecDeliveryDeadLetterSinkRef.Builder | TriggerSpecDeliveryDeadLetterSinkRef,
            ],
            /,
        ) -> "TriggerSpecDeliveryDeadLetterSink.Builder": ...

        @overload
        def ref(
            self, value_or_callback: Never = ...
        ) -> "TriggerSpecDeliveryDeadLetterSinkRef.BuilderContext": ...

        def ref(self, value_or_callback=UNSET, /):
            "Ref points to an Addressable."
            if self._in_context and value_or_callback is UNSET:
                context = TriggerSpecDeliveryDeadLetterSinkRef.BuilderContext()
                context._parent_builder = self
                context._field_name = "ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerSpecDeliveryDeadLetterSinkRef.builder())
                if isinstance(output, TriggerSpecDeliveryDeadLetterSinkRef.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("ref", value)

        def uri(self, value: str | None, /) -> Self:
            "URI can be an absolute URL(non-empty scheme and non-empty host) pointing to the target or a relative URI. Relative URIs will be resolved using the base URI retrieved from Ref."
            return self._set("uri", value)

        def ca_certs(self, value: str | None, /) -> Self:
            "Certification Authority (CA) certificates in PEM format that the source trusts when sending events to the sink."
            return self._set("ca_certs", value)

        def audience(self, value: str | None, /) -> Self:
            "Audience is the OIDC audience. This only needs to be set if the target is not an Addressable and thus the Audience can\u0027t be received from the target itself. If specified, it takes precedence over the target\u0027s Audience."
            return self._set("audience", value)

    class BuilderContext(BuilderContextBase["TriggerSpecDeliveryDeadLetterSink.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerSpecDeliveryDeadLetterSink.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerSpecDeliveryDeadLetterSink."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["TriggerSpecDeliveryDeadLetterSink", Builder]:
        return GenericListBuilder[
            TriggerSpecDeliveryDeadLetterSink, TriggerSpecDeliveryDeadLetterSink.Builder
        ]()

    ref: TriggerSpecDeliveryDeadLetterSinkRef | None = None
    "Ref points to an Addressable."
    uri: str | None = None
    "URI can be an absolute URL(non-empty scheme and non-empty host) pointing to the target or a relative URI. Relative URIs will be resolved using the base URI retrieved from Ref."
    ca_certs: Annotated[str | None, Field(alias="CACerts")] = None
    "Certification Authority (CA) certificates in PEM format that the source trusts when sending events to the sink."
    audience: str | None = None
    "Audience is the OIDC audience. This only needs to be set if the target is not an Addressable and thus the Audience can\u0027t be received from the target itself. If specified, it takes precedence over the target\u0027s Audience."


class TriggerSpecSubscriber(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerSpecSubscriber"]:
            return TriggerSpecSubscriber

        def build(self) -> "TriggerSpecSubscriber":
            return TriggerSpecSubscriber(**self._attrs)

        @overload
        def ref(
            self, value_or_callback: TriggerSpecSubscriberRef | None, /
        ) -> "TriggerSpecSubscriber.Builder": ...

        @overload
        def ref(
            self,
            value_or_callback: Callable[
                [TriggerSpecSubscriberRef.Builder],
                TriggerSpecSubscriberRef.Builder | TriggerSpecSubscriberRef,
            ],
            /,
        ) -> "TriggerSpecSubscriber.Builder": ...

        @overload
        def ref(
            self, value_or_callback: Never = ...
        ) -> "TriggerSpecSubscriberRef.BuilderContext": ...

        def ref(self, value_or_callback=UNSET, /):
            "Ref points to an Addressable."
            if self._in_context and value_or_callback is UNSET:
                context = TriggerSpecSubscriberRef.BuilderContext()
                context._parent_builder = self
                context._field_name = "ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerSpecSubscriberRef.builder())
                if isinstance(output, TriggerSpecSubscriberRef.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("ref", value)

        def uri(self, value: str | None, /) -> Self:
            "URI can be an absolute URL(non-empty scheme and non-empty host) pointing to the target or a relative URI. Relative URIs will be resolved using the base URI retrieved from Ref."
            return self._set("uri", value)

        def ca_certs(self, value: str | None, /) -> Self:
            "Certification Authority (CA) certificates in PEM format that the source trusts when sending events to the sink."
            return self._set("ca_certs", value)

        def audience(self, value: str | None, /) -> Self:
            "Audience is the OIDC audience. This only needs to be set if the target is not an Addressable and thus the Audience can\u0027t be received from the target itself. If specified, it takes precedence over the target\u0027s Audience."
            return self._set("audience", value)

    class BuilderContext(BuilderContextBase["TriggerSpecSubscriber.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerSpecSubscriber.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerSpecSubscriber."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerSpecSubscriber", Builder]:
        return GenericListBuilder[TriggerSpecSubscriber, TriggerSpecSubscriber.Builder]()

    ref: TriggerSpecSubscriberRef | None = None
    "Ref points to an Addressable."
    uri: str | None = None
    "URI can be an absolute URL(non-empty scheme and non-empty host) pointing to the target or a relative URI. Relative URIs will be resolved using the base URI retrieved from Ref."
    ca_certs: Annotated[str | None, Field(alias="CACerts")] = None
    "Certification Authority (CA) certificates in PEM format that the source trusts when sending events to the sink."
    audience: str | None = None
    "Audience is the OIDC audience. This only needs to be set if the target is not an Addressable and thus the Audience can\u0027t be received from the target itself. If specified, it takes precedence over the target\u0027s Audience."


class BrokerSpecDelivery(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerSpecDelivery"]:
            return BrokerSpecDelivery

        def build(self) -> "BrokerSpecDelivery":
            return BrokerSpecDelivery(**self._attrs)

        def backoff_delay(self, value: str | None, /) -> Self:
            "BackoffDelay is the delay before retrying. More information on Duration format: - https://www.iso.org/iso-8601-date-and-time-format.html - https://en.wikipedia.org/wiki/ISO_8601  For linear policy, backoff delay is backoffDelay*\u003cnumberOfRetries\u003e. For exponential policy, backoff delay is backoffDelay*2^\u003cnumberOfRetries\u003e."
            return self._set("backoff_delay", value)

        def backoff_policy(self, value: str | None, /) -> Self:
            "BackoffPolicy is the retry backoff policy (linear, exponential)."
            return self._set("backoff_policy", value)

        @overload
        def dead_letter_sink(
            self, value_or_callback: BrokerSpecDeliveryDeadLetterSink | None, /
        ) -> "BrokerSpecDelivery.Builder": ...

        @overload
        def dead_letter_sink(
            self,
            value_or_callback: Callable[
                [BrokerSpecDeliveryDeadLetterSink.Builder],
                BrokerSpecDeliveryDeadLetterSink.Builder | BrokerSpecDeliveryDeadLetterSink,
            ],
            /,
        ) -> "BrokerSpecDelivery.Builder": ...

        @overload
        def dead_letter_sink(
            self, value_or_callback: Never = ...
        ) -> "BrokerSpecDeliveryDeadLetterSink.BuilderContext": ...

        def dead_letter_sink(self, value_or_callback=UNSET, /):
            "DeadLetterSink is the sink receiving event that could not be sent to a destination."
            if self._in_context and value_or_callback is UNSET:
                context = BrokerSpecDeliveryDeadLetterSink.BuilderContext()
                context._parent_builder = self
                context._field_name = "dead_letter_sink"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerSpecDeliveryDeadLetterSink.builder())
                if isinstance(output, BrokerSpecDeliveryDeadLetterSink.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("dead_letter_sink", value)

        def retry(self, value: int | None, /) -> Self:
            "Retry is the minimum number of retries the sender should attempt when sending an event before moving it to the dead letter sink."
            return self._set("retry", value)

    class BuilderContext(BuilderContextBase["BrokerSpecDelivery.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerSpecDelivery.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerSpecDelivery."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BrokerSpecDelivery", Builder]:
        return GenericListBuilder[BrokerSpecDelivery, BrokerSpecDelivery.Builder]()

    model_config = ConfigDict(
        extra="allow",
    )
    backoff_delay: Annotated[str | None, Field(alias="backoffDelay")] = None
    "BackoffDelay is the delay before retrying. More information on Duration format: - https://www.iso.org/iso-8601-date-and-time-format.html - https://en.wikipedia.org/wiki/ISO_8601  For linear policy, backoff delay is backoffDelay*\u003cnumberOfRetries\u003e. For exponential policy, backoff delay is backoffDelay*2^\u003cnumberOfRetries\u003e."
    backoff_policy: Annotated[str | None, Field(alias="backoffPolicy")] = None
    "BackoffPolicy is the retry backoff policy (linear, exponential)."
    dead_letter_sink: Annotated[
        BrokerSpecDeliveryDeadLetterSink | None, Field(alias="deadLetterSink")
    ] = None
    "DeadLetterSink is the sink receiving event that could not be sent to a destination."
    retry: int | None = None
    "Retry is the minimum number of retries the sender should attempt when sending an event before moving it to the dead letter sink."


class TriggerSpecDelivery(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerSpecDelivery"]:
            return TriggerSpecDelivery

        def build(self) -> "TriggerSpecDelivery":
            return TriggerSpecDelivery(**self._attrs)

        def backoff_delay(self, value: str | None, /) -> Self:
            "BackoffDelay is the delay before retrying. More information on Duration format: - https://www.iso.org/iso-8601-date-and-time-format.html - https://en.wikipedia.org/wiki/ISO_8601  For linear policy, backoff delay is backoffDelay*\u003cnumberOfRetries\u003e. For exponential policy, backoff delay is backoffDelay*2^\u003cnumberOfRetries\u003e."
            return self._set("backoff_delay", value)

        def backoff_policy(self, value: str | None, /) -> Self:
            "BackoffPolicy is the retry backoff policy (linear, exponential)."
            return self._set("backoff_policy", value)

        @overload
        def dead_letter_sink(
            self, value_or_callback: TriggerSpecDeliveryDeadLetterSink | None, /
        ) -> "TriggerSpecDelivery.Builder": ...

        @overload
        def dead_letter_sink(
            self,
            value_or_callback: Callable[
                [TriggerSpecDeliveryDeadLetterSink.Builder],
                TriggerSpecDeliveryDeadLetterSink.Builder | TriggerSpecDeliveryDeadLetterSink,
            ],
            /,
        ) -> "TriggerSpecDelivery.Builder": ...

        @overload
        def dead_letter_sink(
            self, value_or_callback: Never = ...
        ) -> "TriggerSpecDeliveryDeadLetterSink.BuilderContext": ...

        def dead_letter_sink(self, value_or_callback=UNSET, /):
            "DeadLetterSink is the sink receiving event that could not be sent to a destination."
            if self._in_context and value_or_callback is UNSET:
                context = TriggerSpecDeliveryDeadLetterSink.BuilderContext()
                context._parent_builder = self
                context._field_name = "dead_letter_sink"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerSpecDeliveryDeadLetterSink.builder())
                if isinstance(output, TriggerSpecDeliveryDeadLetterSink.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("dead_letter_sink", value)

        def retry(self, value: int | None, /) -> Self:
            "Retry is the minimum number of retries the sender should attempt when sending an event before moving it to the dead letter sink."
            return self._set("retry", value)

        def format(self, value: str | None, /) -> Self:
            "Format is the format used to serialize the event into a http request when delivering the event. It can be json (for structured events), binary (for binary events), or unset."
            return self._set("format", value)

    class BuilderContext(BuilderContextBase["TriggerSpecDelivery.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerSpecDelivery.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerSpecDelivery."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerSpecDelivery", Builder]:
        return GenericListBuilder[TriggerSpecDelivery, TriggerSpecDelivery.Builder]()

    model_config = ConfigDict(
        extra="allow",
    )
    backoff_delay: Annotated[str | None, Field(alias="backoffDelay")] = None
    "BackoffDelay is the delay before retrying. More information on Duration format: - https://www.iso.org/iso-8601-date-and-time-format.html - https://en.wikipedia.org/wiki/ISO_8601  For linear policy, backoff delay is backoffDelay*\u003cnumberOfRetries\u003e. For exponential policy, backoff delay is backoffDelay*2^\u003cnumberOfRetries\u003e."
    backoff_policy: Annotated[str | None, Field(alias="backoffPolicy")] = None
    "BackoffPolicy is the retry backoff policy (linear, exponential)."
    dead_letter_sink: Annotated[
        TriggerSpecDeliveryDeadLetterSink | None, Field(alias="deadLetterSink")
    ] = None
    "DeadLetterSink is the sink receiving event that could not be sent to a destination."
    retry: int | None = None
    "Retry is the minimum number of retries the sender should attempt when sending an event before moving it to the dead letter sink."
    format: str | None = None
    "Format is the format used to serialize the event into a http request when delivering the event. It can be json (for structured events), binary (for binary events), or unset."


class BrokerSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BrokerSpec"]:
            return BrokerSpec

        def build(self) -> "BrokerSpec":
            return BrokerSpec(**self._attrs)

        @overload
        def config(self, value_or_callback: BrokerSpecConfig | None, /) -> "BrokerSpec.Builder": ...

        @overload
        def config(
            self,
            value_or_callback: Callable[
                [BrokerSpecConfig.Builder], BrokerSpecConfig.Builder | BrokerSpecConfig
            ],
            /,
        ) -> "BrokerSpec.Builder": ...

        @overload
        def config(self, value_or_callback: Never = ...) -> "BrokerSpecConfig.BuilderContext": ...

        def config(self, value_or_callback=UNSET, /):
            "Config is a KReference to the configuration that specifies configuration options for this Broker. For example, this could be a pointer to a ConfigMap."
            if self._in_context and value_or_callback is UNSET:
                context = BrokerSpecConfig.BuilderContext()
                context._parent_builder = self
                context._field_name = "config"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerSpecConfig.builder())
                if isinstance(output, BrokerSpecConfig.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("config", value)

        @overload
        def delivery(
            self, value_or_callback: BrokerSpecDelivery | None, /
        ) -> "BrokerSpec.Builder": ...

        @overload
        def delivery(
            self,
            value_or_callback: Callable[
                [BrokerSpecDelivery.Builder],
                BrokerSpecDelivery.Builder | BrokerSpecDelivery,
            ],
            /,
        ) -> "BrokerSpec.Builder": ...

        @overload
        def delivery(
            self, value_or_callback: Never = ...
        ) -> "BrokerSpecDelivery.BuilderContext": ...

        def delivery(self, value_or_callback=UNSET, /):
            "Delivery contains the delivery spec for each trigger to this Broker. Each trigger delivery spec, if any, overrides this global delivery spec."
            if self._in_context and value_or_callback is UNSET:
                context = BrokerSpecDelivery.BuilderContext()
                context._parent_builder = self
                context._field_name = "delivery"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerSpecDelivery.builder())
                if isinstance(output, BrokerSpecDelivery.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("delivery", value)

    class BuilderContext(BuilderContextBase["BrokerSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BrokerSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BrokerSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BrokerSpec", Builder]:
        return GenericListBuilder[BrokerSpec, BrokerSpec.Builder]()

    config: BrokerSpecConfig | None = None
    "Config is a KReference to the configuration that specifies configuration options for this Broker. For example, this could be a pointer to a ConfigMap."
    delivery: BrokerSpecDelivery | None = None
    "Delivery contains the delivery spec for each trigger to this Broker. Each trigger delivery spec, if any, overrides this global delivery spec."


class TriggerSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TriggerSpec"]:
            return TriggerSpec

        def build(self) -> "TriggerSpec":
            return TriggerSpec(**self._attrs)

        def broker(self, value: str | None, /) -> Self:
            "Broker is the broker that this trigger receives events from."
            return self._set("broker", value)

        @overload
        def broker_ref(
            self, value_or_callback: TriggerSpecBrokerRef | None, /
        ) -> "TriggerSpec.Builder": ...

        @overload
        def broker_ref(
            self,
            value_or_callback: Callable[
                [TriggerSpecBrokerRef.Builder],
                TriggerSpecBrokerRef.Builder | TriggerSpecBrokerRef,
            ],
            /,
        ) -> "TriggerSpec.Builder": ...

        @overload
        def broker_ref(
            self, value_or_callback: Never = ...
        ) -> "TriggerSpecBrokerRef.BuilderContext": ...

        def broker_ref(self, value_or_callback=UNSET, /):
            "Reference to a broker that is enabled for cross-namespace referencing. You can specify only the following fields of the KReference: kind, apiVersion, name and namespace."
            if self._in_context and value_or_callback is UNSET:
                context = TriggerSpecBrokerRef.BuilderContext()
                context._parent_builder = self
                context._field_name = "broker_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerSpecBrokerRef.builder())
                if isinstance(output, TriggerSpecBrokerRef.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("broker_ref", value)

        @overload
        def delivery(
            self, value_or_callback: TriggerSpecDelivery | None, /
        ) -> "TriggerSpec.Builder": ...

        @overload
        def delivery(
            self,
            value_or_callback: Callable[
                [TriggerSpecDelivery.Builder],
                TriggerSpecDelivery.Builder | TriggerSpecDelivery,
            ],
            /,
        ) -> "TriggerSpec.Builder": ...

        @overload
        def delivery(
            self, value_or_callback: Never = ...
        ) -> "TriggerSpecDelivery.BuilderContext": ...

        def delivery(self, value_or_callback=UNSET, /):
            "Delivery contains the delivery spec for this specific trigger."
            if self._in_context and value_or_callback is UNSET:
                context = TriggerSpecDelivery.BuilderContext()
                context._parent_builder = self
                context._field_name = "delivery"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerSpecDelivery.builder())
                if isinstance(output, TriggerSpecDelivery.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("delivery", value)

        @overload
        def filter(
            self, value_or_callback: TriggerSpecFilter | None, /
        ) -> "TriggerSpec.Builder": ...

        @overload
        def filter(
            self,
            value_or_callback: Callable[
                [TriggerSpecFilter.Builder],
                TriggerSpecFilter.Builder | TriggerSpecFilter,
            ],
            /,
        ) -> "TriggerSpec.Builder": ...

        @overload
        def filter(self, value_or_callback: Never = ...) -> "TriggerSpecFilter.BuilderContext": ...

        def filter(self, value_or_callback=UNSET, /):
            "Filter is the filter to apply against all events from the Broker. Only events that pass this filter will be sent to the Subscriber. If not specified, will default to allowing all events."
            if self._in_context and value_or_callback is UNSET:
                context = TriggerSpecFilter.BuilderContext()
                context._parent_builder = self
                context._field_name = "filter"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerSpecFilter.builder())
                if isinstance(output, TriggerSpecFilter.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("filter", value)

        @overload
        def filters(
            self, value_or_callback: list[TriggerSpecFiltersItem] | None, /
        ) -> "TriggerSpec.Builder": ...

        @overload
        def filters(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[TriggerSpecFiltersItem, TriggerSpecFiltersItem.Builder]],
                GenericListBuilder[TriggerSpecFiltersItem, TriggerSpecFiltersItem.Builder]
                | list[TriggerSpecFiltersItem],
            ],
            /,
        ) -> "TriggerSpec.Builder": ...

        @overload
        def filters(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[TriggerSpecFiltersItem.Builder]: ...

        def filters(self, value_or_callback=UNSET, /):
            "Filters is an array of SubscriptionsAPIFilter that evaluate to true or false. If any filter expression in the array evaluates to false, the event must not be sent to the Subscriber. If all the filter expressions in the array evaluate to true, the event must be attempted to be delivered. Absence of a filter or empty array implies a value of true. In the event of users specifying both Filter and Filters, then the latter will override the former. This will allow users to try out the effect of the new Filters field without compromising the existing attribute-based Filter and try it out on existing Trigger objects."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[TriggerSpecFiltersItem.Builder]()
                context._parent_builder = self
                context._field_name = "filters"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerSpecFiltersItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("filters", value)

        @overload
        def subscriber(
            self, value_or_callback: TriggerSpecSubscriber | None, /
        ) -> "TriggerSpec.Builder": ...

        @overload
        def subscriber(
            self,
            value_or_callback: Callable[
                [TriggerSpecSubscriber.Builder],
                TriggerSpecSubscriber.Builder | TriggerSpecSubscriber,
            ],
            /,
        ) -> "TriggerSpec.Builder": ...

        @overload
        def subscriber(
            self, value_or_callback: Never = ...
        ) -> "TriggerSpecSubscriber.BuilderContext": ...

        def subscriber(self, value_or_callback=UNSET, /):
            "Subscriber is the addressable that receives events from the Broker that pass the Filter. It is required."
            if self._in_context and value_or_callback is UNSET:
                context = TriggerSpecSubscriber.BuilderContext()
                context._parent_builder = self
                context._field_name = "subscriber"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerSpecSubscriber.builder())
                if isinstance(output, TriggerSpecSubscriber.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("subscriber", value)

    class BuilderContext(BuilderContextBase["TriggerSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TriggerSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TriggerSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TriggerSpec", Builder]:
        return GenericListBuilder[TriggerSpec, TriggerSpec.Builder]()

    model_config = ConfigDict(
        extra="allow",
    )
    broker: str | None = None
    "Broker is the broker that this trigger receives events from."
    broker_ref: Annotated[TriggerSpecBrokerRef | None, Field(alias="brokerRef")] = None
    "Reference to a broker that is enabled for cross-namespace referencing. You can specify only the following fields of the KReference: kind, apiVersion, name and namespace."
    delivery: TriggerSpecDelivery | None = None
    "Delivery contains the delivery spec for this specific trigger."
    filter: TriggerSpecFilter | None = None
    "Filter is the filter to apply against all events from the Broker. Only events that pass this filter will be sent to the Subscriber. If not specified, will default to allowing all events."
    filters: list[TriggerSpecFiltersItem] | None = None
    "Filters is an array of SubscriptionsAPIFilter that evaluate to true or false. If any filter expression in the array evaluates to false, the event must not be sent to the Subscriber. If all the filter expressions in the array evaluate to true, the event must be attempted to be delivered. Absence of a filter or empty array implies a value of true. In the event of users specifying both Filter and Filters, then the latter will override the former. This will allow users to try out the effect of the new Filters field without compromising the existing attribute-based Filter and try it out on existing Trigger objects."
    subscriber: TriggerSpecSubscriber | None = None
    "Subscriber is the addressable that receives events from the Broker that pass the Filter. It is required."


class Broker(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "brokers",
        "scope": "Namespaced",
        "status": True,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Broker"]:
            return Broker

        def build(self) -> "Broker":
            return Broker(**self._attrs)

        @overload
        def spec(self, value_or_callback: BrokerSpec | None, /) -> "Broker.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[[BrokerSpec.Builder], BrokerSpec.Builder | BrokerSpec],
            /,
        ) -> "Broker.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "BrokerSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = BrokerSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerSpec.builder())
                if isinstance(output, BrokerSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: BrokerStatus | None, /) -> "Broker.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [BrokerStatus.Builder], BrokerStatus.Builder | BrokerStatus
            ],
            /,
        ) -> "Broker.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "BrokerStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = BrokerStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BrokerStatus.builder())
                if isinstance(output, BrokerStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

        def api_version(self, value: Literal["eventing.knative.dev/v1"], /) -> Self:
            return self._set("api_version", value)

        def kind(self, value: Literal["Broker"], /) -> Self:
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Broker.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Broker.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

    class BuilderContext(BuilderContextBase["Broker.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Broker.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Broker."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Broker", Builder]:
        return GenericListBuilder[Broker, Broker.Builder]()

    spec: BrokerSpec | None = None
    status: BrokerStatus | None = None
    api_version: Annotated[Literal["eventing.knative.dev/v1"], Field(alias="apiVersion")] = (
        "eventing.knative.dev/v1"
    )
    kind: Literal["Broker"] = "Broker"
    metadata: ObjectMeta | None = None


class Trigger(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "triggers",
        "scope": "Namespaced",
        "status": True,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Trigger"]:
            return Trigger

        def build(self) -> "Trigger":
            return Trigger(**self._attrs)

        @overload
        def spec(self, value_or_callback: TriggerSpec | None, /) -> "Trigger.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[[TriggerSpec.Builder], TriggerSpec.Builder | TriggerSpec],
            /,
        ) -> "Trigger.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "TriggerSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = TriggerSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerSpec.builder())
                if isinstance(output, TriggerSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: TriggerStatus | None, /) -> "Trigger.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [TriggerStatus.Builder], TriggerStatus.Builder | TriggerStatus
            ],
            /,
        ) -> "Trigger.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "TriggerStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = TriggerStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TriggerStatus.builder())
                if isinstance(output, TriggerStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

        def api_version(self, value: Literal["eventing.knative.dev/v1"], /) -> Self:
            return self._set("api_version", value)

        def kind(self, value: Literal["Trigger"], /) -> Self:
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Trigger.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Trigger.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

    class BuilderContext(BuilderContextBase["Trigger.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Trigger.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Trigger."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Trigger", Builder]:
        return GenericListBuilder[Trigger, Trigger.Builder]()

    spec: TriggerSpec | None = None
    status: TriggerStatus | None = None
    api_version: Annotated[Literal["eventing.knative.dev/v1"], Field(alias="apiVersion")] = (
        "eventing.knative.dev/v1"
    )
    kind: Literal["Trigger"] = "Trigger"
    metadata: ObjectMeta | None = None
